# Legacy experiment material

These files are retained for historical reproducibility. They are superseded,
incomplete, or tied to immutable historical run identifiers and should not be
used for new `plasmax` experiments.

`fixed_duration_acceptance.py` is the retired physical-trajectory acceptance
study. Active coverage lives in the fixed-duration stepper and TORAX-reference
integration tests; no nominal controller trajectory is a release gate.
